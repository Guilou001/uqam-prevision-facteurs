[project]
name = "pmf"
version = "1.0.0"
description = "Prévision du chômage américain par six modèles, dont un modèle à facteurs : le travail ECO8086 de 2021, rendu reproductible"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "Guillaume Vaudescal" }]
dependencies = [
    "matplotlib>=3.9",
    "numpy>=2.0",
    "pandas>=2.2",
    "requests>=2.32",
    "statsmodels>=0.14",
    "typer>=0.12",
]

[project.scripts]
pmf = "pmf.cli:app"

[project.urls]
Homepage = "https://github.com/Guilou001/uqam-prevision-facteurs"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pmf"]

[dependency-groups]
dev = ["pypdf>=5.0", "pytest>=8.0", "ruff>=0.6"]

[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
